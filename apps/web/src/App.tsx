// App.tsx - Global app component (currently unused, kept for potential future global providers)
// AlertContainer and initialization moved to Layout.tsx

export default function App() {
  return null
}
